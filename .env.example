# Base64-encoded secret of at least 32 bytes. Replace this value in your local .env file.
JWT_SECRET=replace-with-a-base64-encoded-secret

# Use prod outside local development.
SPRING_PROFILES_ACTIVE=dev
